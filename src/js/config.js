export default {
  name: process.env.NAME, // actual name of the recipient (Mandatory)

  nickname: info.env.NICKNAME, // nickname(optional)

  pic: info.env.PIC, // image url of recipients (Mandatory)

  showScrollMsg: info.env.SCROLL_MSG, // set to false if you do not want the scrolling message

  birthDate: info.env.BIRTH_DATE, // Mention birthday date in YYYY-MM-DD format
};
